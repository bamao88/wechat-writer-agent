---
phase: 02-tool-call-validation-session-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/agent_sdk.py
  - src/hooks/logging_hooks.py
  - .env.example
  - tests/test_streaming_compatibility.py
autonomous: true

must_haves:
  truths:
    - "SDK API calls complete without timeout (API_TIMEOUT_MS configured)"
    - "Async hooks do not block the event loop (no synchronous input())"
    - "MiniMax API receives reasoning_split=True parameter"
    - "Streaming responses are parsed correctly without event sequence errors"
  artifacts:
    - path: "src/modules/agent_sdk.py"
      provides: "Async-safe SDK runner with timeout and streaming compatibility"
      contains: "API_TIMEOUT_MS"
    - path: "tests/test_streaming_compatibility.py"
      provides: "Streaming compatibility verification tests"
      exports: ["test_timeout_configuration", "test_async_hooks_non_blocking"]
  key_links:
    - from: "src/modules/agent_sdk.py"
      to: "claude_agent_sdk.query"
      via: "ClaudeAgentOptions with env vars"
      pattern: "API_TIMEOUT_MS.*3000000"
---

<objective>
Fix MiniMax streaming compatibility issues identified in research report.

Purpose: Enable Claude Agent SDK to work reliably with MiniMax API by addressing timeout, async blocking, and event sequence issues that cause the SDK to hang or fail.

Output: Working SDK configuration that completes API calls without hanging, with proper timeout handling and async-safe hooks.
</objective>

<execution_context>
@/Users/xpw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xpw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-registration-protocol-validation/01-01-SUMMARY.md
@.planning/phases/01-tool-registration-protocol-validation/01-02-SUMMARY.md
@.planning/research/SDK与MiniMax流式传输实现的兼容性架构与工程解决方案深度报告.md

Critical findings from research report:
1. SDK default timeout (30s) is too short for MiniMax M2.1 reasoning tasks
2. Synchronous hooks block async event loop, causing SSE stream freeze
3. MiniMax requires reasoning_split=True for proper thinking content handling
4. API_TIMEOUT_MS environment variable controls SDK timeout
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure streaming timeout and async safety</name>
  <files>src/modules/agent_sdk.py, src/hooks/logging_hooks.py, .env.example</files>
  <action>
    Implement the following changes based on research report recommendations:

    1. In src/modules/agent_sdk.py:
       - Add API_TIMEOUT_MS=3000000 to env_vars dict in generate() method
       - This sets 3000-second timeout to handle MiniMax M2.1 reasoning delays
       - Add comment explaining why this timeout is necessary

    2. In src/hooks/logging_hooks.py:
       - Verify all hooks are async-safe (already async def)
       - Add a docstring warning about NOT using synchronous input() in hooks
       - The existing hooks use print() which is fine - it's non-blocking

    3. In .env.example:
       - Add API_TIMEOUT_MS=3000000 with documentation
       - Add CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 to reduce stream parsing pressure
       - Document these are critical for MiniMax M2.1 compatibility

    Do NOT:
    - Change the core SDK query logic
    - Add LiteLLM proxy (that's a backup strategy)
    - Remove existing functionality
  </action>
  <verify>
    - grep -n "API_TIMEOUT_MS" src/modules/agent_sdk.py shows timeout configured
    - grep -n "API_TIMEOUT_MS" .env.example shows documentation
    - python -c "from src.modules.agent_sdk import AgentSDKRunner; print('Import OK')"
  </verify>
  <done>
    - API_TIMEOUT_MS=3000000 configured in SDK env vars
    - Hooks documented as async-safe
    - .env.example updated with streaming configuration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create streaming compatibility tests</name>
  <files>tests/test_streaming_compatibility.py</files>
  <action>
    Create tests/test_streaming_compatibility.py with the following tests:

    1. test_timeout_configuration():
       - Verify AgentSDKRunner includes API_TIMEOUT_MS in env vars
       - Import AgentSDKRunner and check _generate method adds timeout
       - Mock the environment and verify timeout is passed to SDK

    2. test_async_hooks_non_blocking():
       - Import all hooks from src/hooks/logging_hooks.py
       - Verify they are async functions (inspect.iscoroutinefunction)
       - Run hooks with mock data and verify they complete quickly (<100ms)
       - Use asyncio.timeout to ensure no blocking

    3. test_env_vars_propagation():
       - Create AgentSDKRunner with mock config
       - Verify ClaudeAgentOptions includes correct env vars
       - Check ANTHROPIC_BASE_URL is passed through

    Use pytest-asyncio for async tests.
    Use unittest.mock to avoid actual API calls.
    Add explicit 5-second timeouts to all tests.
  </action>
  <verify>
    pytest tests/test_streaming_compatibility.py -v --timeout=30
    All tests pass without hanging
  </verify>
  <done>
    - 3 streaming compatibility tests created
    - Tests verify timeout configuration
    - Tests verify hooks are async-safe
    - Tests pass with explicit timeouts
  </done>
</task>

</tasks>

<verification>
Run all verification commands:

```bash
# Task 1 verification
grep -n "API_TIMEOUT_MS" src/modules/agent_sdk.py
grep -n "API_TIMEOUT_MS" .env.example
python -c "from src.modules.agent_sdk import AgentSDKRunner; print('Import OK')"

# Task 2 verification
pytest tests/test_streaming_compatibility.py -v --timeout=30
```

All commands should succeed. Tests should complete within 30 seconds.
</verification>

<success_criteria>
1. API_TIMEOUT_MS=3000000 configured in SDK to prevent timeout during MiniMax reasoning
2. Hooks verified as async-safe with no blocking operations
3. .env.example documents streaming configuration requirements
4. All streaming compatibility tests pass
5. No regression in existing tests (pytest tests/ -v --ignore=tests/test_tool_registration.py --timeout=60)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-call-validation-session-history/02-01-SUMMARY.md`
</output>
