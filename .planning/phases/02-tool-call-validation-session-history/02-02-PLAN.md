---
phase: 02-tool-call-validation-session-history
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/modules/agent_sdk.py
  - src/hooks/logging_hooks.py
  - tests/test_tool_call_validation.py
autonomous: true

must_haves:
  truths:
    - "Agent autonomously calls NotebookLM tool when materials are insufficient"
    - "tool_call_count > 0 when tool is invoked (not always 0)"
    - "Tool call logs include tool name, timing, query parameters, result summary"
    - "PreToolUse and PostToolUse hooks capture complete tool call lifecycle"
    - "Trigger conditions verified: empty search_results + prompt instruction = tool call"
  artifacts:
    - path: "src/hooks/logging_hooks.py"
      provides: "Enhanced tool call logging with detailed parameters"
      contains: "query_params"
    - path: "tests/test_tool_call_validation.py"
      provides: "Integration tests verifying tool invocation and trigger conditions"
      exports: ["test_tool_call_count_nonzero", "test_tool_call_logging_complete", "test_trigger_conditions_empty_search_results"]
  key_links:
    - from: "src/hooks/logging_hooks.py"
      to: "metrics.tool_calls"
      via: "PreToolUse/PostToolUse hooks"
      pattern: "tool_calls\\.append"
    - from: "src/modules/agent_sdk.py"
      to: "src/hooks/logging_hooks.py"
      via: "HookMatcher configuration"
      pattern: "pre_tool_use_hook"
    - from: "write_prompt/V1.md"
      to: "Agent tool decision"
      via: "Prompt instruction for NotebookLM usage"
      pattern: "notebooklm|知识库"
---

<objective>
Validate that Agent autonomously calls NotebookLM tool and logging captures complete details.

Purpose: Verify the core requirement that Agent makes intelligent tool call decisions during generation, and that the logging system correctly records these calls (addressing the "tool_call_count always 0" issue).

Output: Integration tests proving tool calls happen autonomously, and enhanced logging capturing tool name, timing, parameters, and result summary.
</objective>

<execution_context>
@/Users/xpw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xpw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-call-validation-session-history/02-01-PLAN.md

Requirements addressed:
- VAL-01: Agent automatically calls NotebookLM tool when materials are insufficient
- VAL-02: tool_call_count > 0 (not always 0)
- VAL-03: Tool call logs include tool name, timing, query parameters, result summary
- VAL-04: Tests can verify tool was actually called (not false positive)

Current state from Phase 1:
- SDK configured with setting_sources and allowed_tools
- NotebookLM Skill discovery enabled
- Hooks exist but need validation they capture tool calls
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance tool call logging with complete details</name>
  <files>src/hooks/logging_hooks.py</files>
  <action>
    Enhance the PreToolUse and PostToolUse hooks to capture complete tool call information:

    1. In pre_tool_use_hook():
       - Add 'query_params' field to capture the actual query string (for NotebookLM, this is the "question" parameter)
       - Add 'invocation_reason' field to document why the tool was called (from context if available)
       - Log more detail: tool name, query, timestamp

    2. In post_tool_use_hook():
       - Add 'result_summary' field (first 200 chars of result for logging)
       - Add 'result_length' field (full result length)
       - Add 'success' boolean field (True if result exists and is not error)
       - Calculate and store duration_ms more precisely

    3. Add a new utility function get_tool_call_summary(metrics) that returns:
       - Total tool call count
       - List of tool names called
       - Average duration
       - Success/failure counts

    4. VERIFY WIRING: Confirm HookMatcher configuration in agent_sdk.py includes these hooks:
       - Check that _build_hooks() returns dict with "PreToolUse" and "PostToolUse" keys
       - Each key must have HookMatcher with pre_tool_use_hook / post_tool_use_hook respectively
       - The hooks must receive metrics parameter via lambda closure

    Maintain backward compatibility - existing fields must still work.
  </action>
  <verify>
    python -c "from src.hooks.logging_hooks import pre_tool_use_hook, post_tool_use_hook, get_tool_call_summary; print('Import OK')"
    grep -n "query_params" src/hooks/logging_hooks.py
    grep -n "result_summary" src/hooks/logging_hooks.py
    # Verify HookMatcher wiring in agent_sdk.py
    grep -n "PreToolUse.*HookMatcher" src/modules/agent_sdk.py
    grep -n "PostToolUse.*HookMatcher" src/modules/agent_sdk.py
    grep -n "pre_tool_use_hook" src/modules/agent_sdk.py
    grep -n "post_tool_use_hook" src/modules/agent_sdk.py
  </verify>
  <done>
    - PreToolUse captures query_params and invocation_reason
    - PostToolUse captures result_summary, result_length, success
    - get_tool_call_summary utility function exists
    - HookMatcher wiring verified: PreToolUse and PostToolUse keys exist in _build_hooks()
    - Backward compatible with existing code
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tool call validation tests</name>
  <files>tests/test_tool_call_validation.py</files>
  <action>
    Create tests/test_tool_call_validation.py with integration tests:

    1. test_tool_call_count_nonzero():
       - Create mock SDK runner with tool calling enabled
       - Simulate a scenario where materials are insufficient (empty search_results)
       - Verify that tool_call_count > 0 after generation
       - Use mock to avoid actual API calls, but verify hook is triggered

    2. test_tool_call_logging_complete():
       - Create metrics instance
       - Call pre_tool_use_hook with sample tool data
       - Call post_tool_use_hook with sample result
       - Verify all required fields are captured:
         * tool_name, tool_use_id, input, timestamp, start_time (from pre)
         * end_time, duration_ms, result, result_summary, success (from post)

    3. test_tool_call_summary_aggregation():
       - Create metrics with multiple tool calls
       - Call get_tool_call_summary()
       - Verify count, names list, average duration are correct

    4. test_pre_post_hook_matching():
       - Verify pre and post hooks correctly match by tool_use_id
       - Multiple concurrent tool calls should not interfere

    Use pytest-asyncio for async tests.
    Use unittest.mock to simulate SDK behavior.
    Add explicit 10-second timeouts.
  </action>
  <verify>
    pytest tests/test_tool_call_validation.py -v --timeout=60
    All tests pass
  </verify>
  <done>
    - 4 tool call validation tests created
    - Tests verify tool_call_count > 0 scenario
    - Tests verify complete logging fields
    - Tests verify summary aggregation
    - All tests pass with timeouts
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify autonomous tool call trigger conditions (VAL-01)</name>
  <files>tests/test_tool_call_validation.py, src/modules/agent_sdk.py</files>
  <action>
    Add tests that verify the CONDITIONS that trigger autonomous tool calling:

    1. test_trigger_conditions_empty_search_results():
       - Create AgentSDKRunner with allowed_tools including "Skill"
       - Verify that when search_results is empty/None, the prompt system message
         instructs the Agent to query NotebookLM for knowledge
       - This tests the TRIGGER: empty materials -> Agent decides to call tool

    2. test_trigger_conditions_prompt_contains_tool_instruction():
       - Load the prompt template (write_prompt/V1.md)
       - Verify it contains instruction for using NotebookLM when lacking materials
       - Pattern: should mention "notebooklm" or "知识库" and "查询"/"query"

    3. test_trigger_conditions_allowed_tools_configured():
       - Verify AgentSDKRunner includes "Skill" in allowed_tools
       - Verify setting_sources includes "user" for skill discovery
       - These are prerequisites for autonomous tool calling

    This directly tests VAL-01's trigger mechanism:
    - Agent AUTOMATICALLY calls tool (trigger: prompt + empty materials)
    - Not manual calling (no user intervention needed)
  </action>
  <verify>
    pytest tests/test_tool_call_validation.py::test_trigger_conditions_empty_search_results -v --timeout=30
    pytest tests/test_tool_call_validation.py::test_trigger_conditions_prompt_contains_tool_instruction -v --timeout=30
    pytest tests/test_tool_call_validation.py::test_trigger_conditions_allowed_tools_configured -v --timeout=30
  </verify>
  <done>
    - Tests verify empty search_results triggers tool query decision
    - Tests verify prompt template contains tool usage instructions
    - Tests verify SDK has correct allowed_tools and setting_sources
    - VAL-01 trigger mechanism is now testable (not just behavior)
  </done>
</task>

</tasks>

<verification>
Run all verification commands:

```bash
# Task 1 verification
python -c "from src.hooks.logging_hooks import pre_tool_use_hook, post_tool_use_hook, get_tool_call_summary; print('Import OK')"
grep -n "query_params" src/hooks/logging_hooks.py
grep -n "result_summary" src/hooks/logging_hooks.py
grep -n "get_tool_call_summary" src/hooks/logging_hooks.py
# Verify HookMatcher wiring
grep -n "pre_tool_use_hook" src/modules/agent_sdk.py
grep -n "post_tool_use_hook" src/modules/agent_sdk.py

# Task 2 + Task 3 verification
pytest tests/test_tool_call_validation.py -v --timeout=60
```

All commands should succeed.
</verification>

<success_criteria>
1. PreToolUse hook captures query_params field (VAL-03)
2. PostToolUse hook captures result_summary field (VAL-03)
3. get_tool_call_summary function aggregates tool call data
4. HookMatcher wiring verified in agent_sdk.py (key_links planned)
5. test_tool_call_count_nonzero proves count can be > 0 (VAL-02)
6. test_tool_call_logging_complete verifies all fields (VAL-03)
7. test_trigger_conditions_* tests verify VAL-01 trigger mechanism
8. Tests can distinguish real tool calls from false positives (VAL-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-call-validation-session-history/02-02-SUMMARY.md`
</output>
