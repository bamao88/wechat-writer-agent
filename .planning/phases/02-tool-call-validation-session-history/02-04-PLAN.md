---
phase: 02-tool-call-validation-session-history
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - tests/test_e2e_tool_calling.py
  - scripts/validate_tool_calling.py
autonomous: false

must_haves:
  truths:
    - "End-to-end test triggers actual NotebookLM tool call"
    - "Tool call returns knowledge content that appears in generated article"
    - "Integration test can be replayed to verify tool calling works"
    - "Diagnostic script confirms tool calling mechanism"
  artifacts:
    - path: "tests/test_e2e_tool_calling.py"
      provides: "End-to-end tool calling integration test"
      exports: ["test_autonomous_tool_call_e2e"]
    - path: "scripts/validate_tool_calling.py"
      provides: "Standalone tool calling diagnostic"
      min_lines: 100
  key_links:
    - from: "tests/test_e2e_tool_calling.py"
      to: "src/modules/agent_sdk.py"
      via: "AgentSDKRunner.generate"
      pattern: "generate.*tool"
---

<objective>
Create end-to-end integration test proving tool calling works in real scenarios.

Purpose: Provide conclusive evidence that the Agent autonomously calls tools and uses returned knowledge in generation. This addresses VAL-01 (autonomous calling) and VAL-04 (verifiable not false positive).

Output: End-to-end test that actually invokes NotebookLM tool and verifies the knowledge appears in output, plus diagnostic script for manual verification.
</objective>

<execution_context>
@/Users/xpw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xpw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-call-validation-session-history/02-01-PLAN.md
@.planning/phases/02-tool-call-validation-session-history/02-02-PLAN.md
@.planning/phases/02-tool-call-validation-session-history/02-03-PLAN.md

Requirements addressed:
- VAL-01: Agent automatically calls NotebookLM tool (not manual)
- VAL-04: Verifiable tool call (not false positive)

Success criteria from roadmap:
1. Agent in lacking materials automatically calls NotebookLM tool (no human intervention)
2. Integration test can reproduce tool call and verify returned knowledge is used
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone tool calling diagnostic</name>
  <files>scripts/validate_tool_calling.py</files>
  <action>
    Create scripts/validate_tool_calling.py as a comprehensive diagnostic script:

    1. Test 1: NotebookLM Skill Discovery
       - Check if ~/.claude/skills/notebooklm/ exists
       - Check if skill manifest is valid
       - Report skill status

    2. Test 2: SDK Configuration
       - Load AgentSDKRunner
       - Verify allowed_tools includes "Skill"
       - Verify setting_sources includes "user"
       - Verify API_TIMEOUT_MS is configured

    3. Test 3: Minimal Tool Call (if API available)
       - Create a minimal prompt that requires tool use
       - Use topic: "需要查询知识库获取案例"
       - Pass empty search_results to force tool calling
       - Timeout after 60 seconds
       - Report whether tool was called

    4. Output clear diagnostics:
       - PASS/FAIL for each test
       - Detailed error messages
       - Suggested fixes for failures

    Script should be runnable standalone:
    ```bash
    python scripts/validate_tool_calling.py
    ```

    Use asyncio for async operations.
    Use httpx for direct API calls (bypass SDK if needed).
    Handle all exceptions gracefully.
  </action>
  <verify>
    python scripts/validate_tool_calling.py --help 2>/dev/null || python scripts/validate_tool_calling.py
    Script runs and produces diagnostic output
  </verify>
  <done>
    - Diagnostic script tests skill discovery
    - Diagnostic script tests SDK configuration
    - Diagnostic script attempts minimal tool call
    - Clear PASS/FAIL output for each test
  </done>
</task>

<task type="auto">
  <name>Task 2: Create end-to-end tool calling test</name>
  <files>tests/test_e2e_tool_calling.py</files>
  <action>
    Create tests/test_e2e_tool_calling.py with realistic integration tests:

    1. test_autonomous_tool_call_e2e():
       - Create AgentSDKRunner with valid configuration
       - Use topic that clearly needs knowledge: "请根据我的个人经历写一篇关于创业的文章"
       - Pass empty search_results to force tool calling
       - Run generate() with 120-second timeout
       - Assert tool_call_count > 0
       - Assert metrics.tool_calls[0]['tool_name'] contains 'notebooklm' (case insensitive)
       - Mark as pytest.mark.integration (skip in unit test runs)

    2. test_tool_knowledge_appears_in_output():
       - Similar setup to test 1
       - After generation, check if result_text contains any content from tool results
       - This proves the knowledge was actually used, not just called

    3. test_tool_call_metrics_complete():
       - Run generation with tool calling
       - Verify metrics contains:
         * tool_call_count > 0
         * tool_calls[].tool_name
         * tool_calls[].duration_ms
         * tool_calls[].result_summary

    Use pytest markers:
    - @pytest.mark.integration for tests requiring API
    - @pytest.mark.timeout(180) for long-running tests
    - @pytest.mark.skipif for conditional execution

    Handle API unavailability gracefully (skip with clear message).
  </action>
  <verify>
    # Verify test file exists and contains expected tests
    pytest tests/test_e2e_tool_calling.py --collect-only -q 2>/dev/null | grep -c "test_" || echo "Test collection failed"
    # Verify at least 3 test functions are defined
    grep -c "def test_" tests/test_e2e_tool_calling.py
    # Note: Full integration tests require API access. Run manually:
    # pytest tests/test_e2e_tool_calling.py -v -s --timeout=300 -m integration
  </verify>
  <done>
    - test_autonomous_tool_call_e2e proves autonomous calling
    - test_tool_knowledge_appears_in_output proves knowledge usage
    - test_tool_call_metrics_complete verifies logging
    - Tests marked appropriately for integration runs
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    End-to-end tool calling validation infrastructure:
    - Standalone diagnostic script (scripts/validate_tool_calling.py)
    - Integration tests (tests/test_e2e_tool_calling.py)
    - Enhanced logging from Plans 02-02
  </what-built>
  <how-to-verify>
    1. Run the diagnostic script:
       ```bash
       python scripts/validate_tool_calling.py
       ```
       Expected: At least Tests 1 and 2 should PASS

    2. If API credentials are valid, run integration test:
       ```bash
       pytest tests/test_e2e_tool_calling.py::test_autonomous_tool_call_e2e -v -s --timeout=300
       ```
       Expected: tool_call_count > 0 in output

    3. Check the generated log for tool call details:
       - Should show [PRE-TOOL] and [POST-TOOL] logs
       - Should show tool name (notebooklm related)
  </how-to-verify>
  <resume-signal>Type "approved" if tool calling works, or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
Run all verification commands:

```bash
# Task 1 verification
python scripts/validate_tool_calling.py

# Task 2 verification (test file structure)
pytest tests/test_e2e_tool_calling.py --collect-only -q 2>/dev/null | head -20
grep -c "def test_" tests/test_e2e_tool_calling.py  # Should be >= 3

# Full integration tests (requires API access, run manually):
# pytest tests/test_e2e_tool_calling.py -v -s --timeout=300 -m integration
```
</verification>

<success_criteria>
1. Diagnostic script runs and reports skill discovery status (VAL-04)
2. Diagnostic script verifies SDK configuration
3. Integration test proves tool_call_count > 0 (VAL-01, VAL-02)
4. Integration test proves knowledge appears in output
5. Human verification confirms tool calling works in practice
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-call-validation-session-history/02-04-SUMMARY.md`
</output>
