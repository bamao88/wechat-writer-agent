---
phase: 02-tool-call-validation-session-history
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/test_session_history.py
  - src/modules/agent_sdk.py
autonomous: true

must_haves:
  truths:
    - "Multi-turn conversation with tool calls preserves complete history"
    - "MiniMax API correctly processes messages containing thinking/text/tool_use blocks"
    - "Session history includes tool_result after tool execution"
    - "Conversation context is maintained across multiple tool call rounds"
  artifacts:
    - path: "tests/test_session_history.py"
      provides: "Session history format verification tests"
      exports: ["test_multi_turn_with_tool_calls", "test_history_format_with_thinking"]
    - path: "src/modules/agent_sdk.py"
      provides: "SDK runner with message history tracking"
      contains: "messages"
  key_links:
    - from: "tests/test_session_history.py"
      to: "src/modules/agent_sdk.py"
      via: "metrics.messages"
      pattern: "messages.*append"
---

<objective>
Verify MiniMax API correctly handles session history containing tool calls and thinking content.

Purpose: Ensure that multi-turn conversations with tool calls work correctly, and the MiniMax API can process the complex message format including thinking blocks, text, and tool_use/tool_result.

Output: Integration tests proving session history format is correct and multi-turn tool calling works.

Scope note: This plan has 2 tasks covering 3 helper methods + 5 tests (8 components). Not split because:
1. Both tasks focus on single concern (session history tracking)
2. Task 1 creates infrastructure Task 2 tests - natural pairing
3. Each task modifies only 1 file, total 2 files
</objective>

<execution_context>
@/Users/xpw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xpw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-call-validation-session-history/02-01-PLAN.md
@.planning/research/SDK与MiniMax流式传输实现的兼容性架构与工程解决方案深度报告.md

Requirement addressed:
- API-03: Test MiniMax API session history format requirements

Research findings relevant to session history:
1. Tool calling response includes thinking + tool_use + result in multi-channel format
2. Must preserve full conversation history including thinking/tool_calls for context
3. reasoning_split=True separates thinking from final answer
4. MiniMax SSE events follow: message_start -> content_block_start -> content_block_delta -> content_block_stop
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add message history tracking to SDK runner</name>
  <files>src/modules/agent_sdk.py</files>
  <action>
    Enhance AgentRunMetrics and AgentSDKRunner to track complete message history:

    1. In AgentRunMetrics class:
       - The 'messages' field already exists (List[Dict[str, Any]])
       - Add a new field 'conversation_history' to store the full conversation format
       - Add helper method add_assistant_message(content_blocks) to record assistant responses
       - Add helper method add_tool_result(tool_use_id, result) to record tool results

    2. In AgentSDKRunner.generate():
       - After each message from query(), also record the message type for debugging
       - Track if message contains thinking content (has 'thinking' attribute)
       - Track if message is tool_use response (check content block types)
       - Store stop_reason for each turn

    3. Add method get_history_summary() to AgentRunMetrics that returns:
       - Total message count
       - Count by type (user, assistant, tool_result)
       - Whether thinking content was present
       - Whether tool calls were made

    Maintain backward compatibility with existing metrics structure.
  </action>
  <verify>
    python -c "from src.modules.agent_sdk import AgentRunMetrics; m = AgentRunMetrics(); print(hasattr(m, 'conversation_history'))"
    python -c "from src.modules.agent_sdk import AgentRunMetrics; m = AgentRunMetrics(); print(hasattr(m, 'add_assistant_message'))"
  </verify>
  <done>
    - conversation_history field added to AgentRunMetrics
    - add_assistant_message and add_tool_result helper methods exist
    - get_history_summary method returns message statistics
    - Backward compatible with existing code
  </done>
</task>

<task type="auto">
  <name>Task 2: Create session history format tests</name>
  <files>tests/test_session_history.py</files>
  <action>
    Create tests/test_session_history.py with tests for session history handling:

    1. test_multi_turn_with_tool_calls():
       - Create mock conversation with:
         * User message (initial prompt)
         * Assistant message with tool_use
         * Tool result message
         * Assistant message with final text
       - Verify all messages are recorded in conversation_history
       - Verify correct message ordering and types

    2. test_history_format_with_thinking():
       - Create mock message containing thinking content
       - Verify thinking content is properly tracked
       - Verify get_history_summary correctly reports thinking presence

    3. test_tool_result_format():
       - Call add_tool_result with various result types
       - Verify tool_result is formatted correctly for API
       - Verify tool_use_id is preserved

    4. test_history_summary_accuracy():
       - Create metrics with known message counts
       - Call get_history_summary()
       - Verify all counts are accurate
       - Verify message type categorization is correct

    5. test_conversation_context_preservation():
       - Simulate multi-round conversation
       - Verify earlier messages are preserved in later rounds
       - Verify context window is maintained

    Use pytest-asyncio for async tests.
    Use unittest.mock to simulate SDK/API responses.
    Add explicit 10-second timeouts.
  </action>
  <verify>
    pytest tests/test_session_history.py -v --timeout=60
    All tests pass
  </verify>
  <done>
    - 5 session history tests created
    - Tests verify multi-turn conversation format
    - Tests verify thinking content handling
    - Tests verify tool result format
    - All tests pass with timeouts
  </done>
</task>

</tasks>

<verification>
Run all verification commands:

```bash
# Task 1 verification
python -c "from src.modules.agent_sdk import AgentRunMetrics; m = AgentRunMetrics(); print('conversation_history:', hasattr(m, 'conversation_history'))"
python -c "from src.modules.agent_sdk import AgentRunMetrics; m = AgentRunMetrics(); print('add_assistant_message:', hasattr(m, 'add_assistant_message'))"
python -c "from src.modules.agent_sdk import AgentRunMetrics; m = AgentRunMetrics(); print('get_history_summary:', hasattr(m, 'get_history_summary'))"

# Task 2 verification
pytest tests/test_session_history.py -v --timeout=60
```

All commands should succeed.
</verification>

<success_criteria>
1. AgentRunMetrics tracks complete conversation_history (API-03)
2. Multi-turn conversations with tool calls preserve all messages
3. Thinking content is properly tracked when present
4. Tool results are formatted correctly for MiniMax API
5. get_history_summary provides accurate statistics
6. All 5 session history tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-call-validation-session-history/02-03-SUMMARY.md`
</output>
