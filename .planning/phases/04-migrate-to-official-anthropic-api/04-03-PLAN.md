---
phase: 04-migrate-to-official-anthropic-api
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - docs/README.md
autonomous: false

must_haves:
  truths:
    - "User can verify official API works with real API key"
    - "All integration tests pass on official API"
    - "Documentation reflects official API as primary"
  artifacts:
    - path: "docs/README.md"
      provides: "Updated setup instructions"
      contains: "platform.claude.com"
  key_links:
    - from: "docs/README.md"
      to: ".env.example"
      via: "configuration reference"
      pattern: "\\.env\\.example"
---

<objective>
Final verification and documentation update for official API migration

Purpose: Ensure migration is complete and documented. User verifies with real API key that everything works.

Output:
- Updated docs/README.md with official API setup instructions
- Human verification that tool calling works on official API
</objective>

<execution_context>
@/Users/xpw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xpw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-migrate-to-official-anthropic-api/04-RESEARCH.md

# Prior plan summaries
@.planning/phases/04-migrate-to-official-anthropic-api/04-01-SUMMARY.md
@.planning/phases/04-migrate-to-official-anthropic-api/04-02-SUMMARY.md

# Documentation to update
@docs/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/README.md for official API</name>
  <files>docs/README.md</files>
  <action>
Read current docs/README.md and update the API configuration section:

1. Find the API/configuration section
2. Update to reflect official Anthropic API as primary:

Add or update section:
```markdown
## API 配置

### 官方 Anthropic API (推荐)

1. 获取 API Key: https://platform.claude.com/settings/keys
2. 复制 `.env.example` 为 `.env`
3. 设置 `ANTHROPIC_API_KEY=your-api-key`

```bash
cp .env.example .env
# 编辑 .env 填入你的 API Key
```

### 验证配置

```bash
# 运行简单测试验证 API 连接
python -c "from anthropic import Anthropic; c = Anthropic(); print('API OK')"
```

### MiniMax API (备用)

如需使用 MiniMax API，参见 `.env.example` 中的 "备用配置" 部分。
```

3. Remove or update any outdated MiniMax-specific instructions that suggest it's the primary option

4. Ensure the README reflects the current state:
   - Official API is default
   - Tool calling via Claude Agent SDK
   - NotebookLM skill integration
  </action>
  <verify>
Run: `grep -i "anthropic" docs/README.md | head -5` - should show official API references

Run: `grep -i "platform.claude.com" docs/README.md` - should find at least one reference
  </verify>
  <done>
- docs/README.md updated with official API instructions
- platform.claude.com link included for API key
- MiniMax mentioned only as fallback option
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete API migration: official Anthropic API as primary, with dual-mode support and updated documentation</what-built>
  <how-to-verify>
**Note:** If you don't have an official Anthropic API key yet, you can skip steps 2-3 and proceed with "approved-no-key". The backward compatibility with MiniMax is already verified by automated tests in 04-02.

1. **Prepare your official Anthropic API key (if available):**
   - Get key from: https://platform.claude.com/settings/keys

2. **Update your .env file:**
   ```bash
   cp .env.example .env
   # Edit .env and set:
   # ANTHROPIC_API_KEY=your-real-anthropic-key
   # Remove or comment out ANTHROPIC_BASE_URL if set
   ```

3. **Verify API connectivity (requires real API key):**
   ```bash
   python -c "
   from anthropic import Anthropic
   import os
   client = Anthropic(api_key=os.getenv('ANTHROPIC_API_KEY'))
   response = client.messages.create(
       model='claude-sonnet-4-20250514',
       max_tokens=100,
       messages=[{'role': 'user', 'content': 'Say hello in one word'}]
   )
   print('API Response:', response.content[0].text)
   "
   ```
   Expected: API returns a response (e.g., "Hello!")

4. **Run migration tests (no API key required):**
   ```bash
   pytest tests/test_api_migration.py -v
   ```
   Expected: All tests pass

5. **Run full test suite:**
   ```bash
   pytest tests/ -v --tb=short -x --ignore=tests/test_e2e.py
   ```
   Expected: No regressions from migration

6. **Optional - Test tool calling (if NotebookLM configured):**
   ```bash
   python -c "
   import asyncio
   from src.modules.generator import generate_with_sdk
   # This will attempt tool calling if NOTEBOOK_ID is set
   "
   ```
  </how-to-verify>
  <resume-signal>
Type one of:
- "approved" - if official API works correctly with real key
- "approved-no-key" - if skipping API key verification (backward compat confirmed via tests)
- Describe any issues encountered
  </resume-signal>
</task>

</tasks>

<verification>
Final verification checklist (MIG-01 through MIG-04):

- [ ] MIG-01: System uses official Anthropic API (api.anthropic.com) - verified by human test or automated tests
- [ ] MIG-02: MiniMax-specific code is conditional - tested in test_api_migration.py
- [ ] MIG-03: Tool calling mechanism works - verified if NotebookLM configured
- [ ] MIG-04: All integration tests pass - pytest suite passes
- [ ] MIG-05: Documentation updated - README reflects official API
</verification>

<success_criteria>
- [ ] docs/README.md updated with official API instructions
- [ ] Human verified API connectivity with real Anthropic API key OR approved with "approved-no-key" (backward compat confirmed)
- [ ] pytest tests/test_api_migration.py passes
- [ ] No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-migrate-to-official-anthropic-api/04-03-SUMMARY.md`
</output>
